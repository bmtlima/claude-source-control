please read this repo for context. I'd like your help with something:                                                              
  1. usually, when im using source control and add stuff, after i commit any changes, there's a 'Sync Changes' button. For some      
  reason tho, on this extension, under the agent-maxxing tab on Source Control, after i commit changes they simply vanish. They i    
  need to manually push them on the terminal. Why?                                                                                   
  2. On a similar light, on the other claude source control tabs, there's not blue rectangle under the commit-message rectangle      
  saying 'Commit'. It's only the tickmark. Is it possible to change that? 

seems like doesnt work. Regardless, just need to:
1. understand why tf we cant have the 'sync' button working.
2. Maybe change the UI to have this separate sync button? idk

improve UI cuz rn pretty ugly

some way of seeing which terminal is which

after i removed links.txt it still shows up on Claude: Rainbow